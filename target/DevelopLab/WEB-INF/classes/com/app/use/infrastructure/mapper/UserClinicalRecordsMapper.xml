<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.use.infrastructure.dao.UserClinicalRecordsDao">

  <select id="findById" parameterType="string" resultType="com.app.use.entity.UserClinicalRecords">
    SELECT
    	*
    FROM
    	user_clinical_records
    WHERE
    	id = #{id}
  </select>

  <update id="update" parameterType="com.app.use.entity.UserClinicalRecords">
	UPDATE
	  user_clinical_records
	SET
	<if test="gender != null">
	  gender = #{gender},
	</if>
	<if test="age != null">
	  age = #{age},
	</if>
	<if test="birthday != null">
	  birthday = #{birthday},
	</if>
	<if test="sex != null">
	  sex = #{sex},
	</if>
	<if test="personality != null">
	  personality = #{personality},
	</if>
	<if test="intimacy != null">
	  intimacy = #{intimacy},
	</if>
	<if test="psychopath != null">
	  psychopath = #{psychopath},
	</if>
	  update_date = NOW()
	WHERE
	  id = #{id}
  </update>
</mapper>