<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.use.infrastructure.dao.VoteInfoUserDao">

  <select id="findVoteInfoUser" parameterType="com.app.use.entity.VoteInfoUser" resultType="com.app.use.entity.VoteInfoUser">
    SELECT
    	*
    FROM
    	vote_info_user
    WHERE
    	vote_id = ${vote_id}
    	AND vote_number = ${vote_number}
  </select>

  <update id="insert" parameterType="com.app.use.entity.VoteInfoUser">
	INSERT INTO vote_info_user (
		 vote_id
		, vote_number
		, user_name
		, create_date
		, update_date
	) VALUES (
		${vote_id}
		, ${vote_number}
		, #{user_name}
		, NOW()
		, NOW()
	)
  </update>

</mapper>